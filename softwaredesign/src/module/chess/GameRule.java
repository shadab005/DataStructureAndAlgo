package module.chess;

/**
 * 
 * Responsible for checking if the move being played is the valid one or not.
 * Note : This class doesn't dictate what will happen when the piece is moved to the position, ex: Wha 
 *
 */
public class GameRule {

	public boolean isValid(Board board, Move move, PieceColor playerPieceColor) {
		return true;
	}
	
	
	

}
